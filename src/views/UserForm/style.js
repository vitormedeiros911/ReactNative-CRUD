import { StyleSheet } from "react-native";

export default styles =  StyleSheet.create({
  form: {
    padding: 12,
  },

  input: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1,
    marginBottom: 10
  }
})
